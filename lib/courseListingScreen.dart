import 'package:flutter/material.dart';

class CourseListingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Course Listing'),
      ),
      body: Center(
        child: Text('Course Listing Screen Content Goes Here'),
      ),
    );
  }
}
