import 'package:flutter/material.dart';

class WordGeneratorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Word Generator'),
      ),
      body: Center(
        child: Text('Word Generator Screen Content Goes Here'),
      ),
    );
  }
}
